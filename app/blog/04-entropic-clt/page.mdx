import { PostLayout, metad, Theorem } from '../components';

export const meta = {
  title: "Entropic Central Limit Theorem",
  date: "September 10th, 2023",
  description: "Entropic CLT"
};

export const metadata = metad(meta);

## Testing 123

HEllo

export default ({ children }) => <PostLayout meta={meta}>{children}</PostLayout>

